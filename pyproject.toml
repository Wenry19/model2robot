[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "model2robot"
version = "1.0.0"
description = "Machine learning tools for model2robot"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
